/* The chase list, inside Michael's rail. One screen, two addresses: the old
   one (/compliance/tracker) is in emails and bookmarks and keeps working. */
export { default } from "../../compliance/tracker/page";
