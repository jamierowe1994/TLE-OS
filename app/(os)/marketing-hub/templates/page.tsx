/**
 * Marketing, Email templates: every email the company sends, previewed as
 * the recipient sees it and editable in the builder. The same screen as
 * Admin, Emails - one catalogue, two doors - so Francesca can rework the
 * maintenance, invoice and doorway emails without an owner's login
 * (James, 7 Sep 2026).
 */
export { default } from "@/app/(os)/admin/emails/page";
